"use client";

/**
 * AIChat - Floating AI chat assistant
 * Currently hidden — will be enabled when AI chat feature is implemented.
 * Keeping the component to maintain import references and avoid build breaks.
 */
export default function AIChat() {
  return null;
}
